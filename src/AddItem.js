import React from 'react';
import { PermissionsAndroid, StyleSheet } from 'react-native';
import * as fs from 'react-native-fs';

const AddItem = async ({setItems}) => {
  
}

const styles = StyleSheet.create({
  addItem: {
    position: 'absolute',
    top: '0%',
    left: '0%',
    width: '100%',
    height: '100%',
  }
});

export default AddItem;